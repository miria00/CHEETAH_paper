\begin{landscape}
\section{The Master CHEETAH-V1 Program: A Comprehensive Reference}
\label{app:master_v3}

For reference, we collect the complete comprehensive CHEETAH-V1 co-design program
in one place: the core program on the first page and its extended families on the
second. It is one certified optimization over a generic graph
intermediate representation (a DAG of operations, each carrying a Timeloop-derived
tile-configuration Pareto set and a roofline, connected by residency edges), with
the hardware sized \emph{inside} the program and every serving-regime decision folded
in as a certified constraint family. Bandwidth \emph{is} a finite menu of legal
points, so its reciprocal $u=1/\mathrm{BW}$ is an exact one-hot expression
whose LP relaxation is a lower bound; the compute reciprocal $v=1/\mathrm{MACs}$
is bracketed by tangent rows (outer, certified lower bound) and chord rows
(inner, an achievable design only once every selector is pinned and the
exact-lift check passes); and every selector-times-quantity product is written
in convex-hull form, exact at integral selectors. The program is therefore a
\emph{linear program} (LP): hull rows for the products, epigraph rows for the
rooflines, and linear rows for everything else. Each constraint family is drawn
from a production serving or kernel system (vLLM, SGLang, ThunderKittens,
TensorRT-LLM); \Cref{app:lp_provenance} maps each technique to its constraint
family, names the solver, and reports a preliminary serving-regime validation on
DeepSeek-V3. Each family is \emph{inert when pinned}, so
\eqref{eq:v0} $\subseteq$ \eqref{eq:v1} $\subseteq$ (this program).
The extended families below \emph{are part of this program}, not a wish list
appended to it: each is written out as the rows the solver would receive, and
each carries a status marker separating the families whose effect this paper
measures from those the formulation carries but has not yet measured on the
suite. We state them here in full because the program, not any one experiment,
is what a reader should be able to reproduce and build on.

\begin{center}
\footnotesize
\setlength{\arraycolsep}{1.5pt}
\fbox{%
\begin{minipage}{0.965\linewidth}
\centering
\vspace{2pt}
\textbf{\normalsize The Comprehensive CHEETAH-V1 Program}\\[2pt]
\[
\min\;\; T_{\mathrm{serve}}
\;+\; \sum_{i,t} c_i^{\mathrm{remat}}\, r_{i,t}
\;+\; \varepsilon_{\mathrm{CGM}}\sum_{k} k\, h_{\mathrm{GM},k}
\qquad\text{s.t.}
\]
\vspace{-6pt}
\hrule
\vspace{4pt}
%
\begin{minipage}[t]{0.485\linewidth}
\underline{\textsc{Selection \& exact coupling}}
\vspace{-2pt}
\begin{align*}
&\textstyle\sum_c y_{i,c}=1,\quad \sum_k h_{\mathrm{GM},k}=1\\[-1pt]
&w_{i,c,k}=y_{i,c}\,p_{\mathrm{assoc}(i,k),o(i)}\ \text{[McCormick hull, exact at integral $y$]}
\end{align*}
\vspace{-8pt}

\underline{\textsc{Disaggregated perspective epigraph (roofline)}}
\vspace{-2pt}
\begin{align*}
&\textstyle\sum_c v_{i,c}=v,\ \ \underline v\,y_{i,c}\le v_{i,c}\le\overline v\,y_{i,c},\ \ \sum_{a,f} v^{a,f}_{i,c}=v_{i,c},\ \ 0\le v^{a,f}_{i,c}\le\overline v\,\psi_{a,f}\\[-1pt]
&\tau_{i,c}\ \ge\ \textstyle\sum_{a,f} \frac{\Tmin_i(c)}{\eta_{i,a}\,\gamma_f}\,v^{a,f}_{i,c}\ \ [\text{per-branch copies of }v\text{; lower bound on }y\text{ only}]\\[-2pt]
&X_{i,c}=\Tmax_i(c)\,y_{i,c}-\textstyle\sum_k t_i^k(c)\,w_{i,c,k}=\sum_j X_{i,c,j},\quad \underline X_{i,c}\,\nu_j\le X_{i,c,j}\le\Tmax_i(c)\,\nu_j\\[-2pt]
&\tau_{i,c}\ \ge\ \textstyle\sum_j u_j\,X_{i,c,j}\ \ [\text{bandwidth-menu copies}],\qquad T_i\ge\textstyle\sum_c\tau_{i,c}
\end{align*}
\vspace{-8pt}

\underline{\textsc{Interleaved prefill+decode serving} \citep{kwon2023vllm}}
\vspace{-2pt}
\begin{align*}
&T_{\mathrm{serve}}\ge\theta\,C^{\mathrm{pre}}+N\,C^{\mathrm{dec}},\quad
 T_{\mathrm{serve}}\ge\theta\,M^{\mathrm{pre}}+N\,M^{\mathrm{dec}}\quad[\text{lifted streams; }\theta\text{ a constant}]
\end{align*}
\vspace{-8pt}

\underline{\textsc{Hardware sizing, the chip inside the LP}}
\vspace{-2pt}
\begin{align*}
&v\ge \tfrac{2}{a_g}-\tfrac{m}{a_g^{2}}\ \forall g\ \ [\text{tangent rows; chord rows for the achievable LP}]\\[-1pt]
&\mathrm{BW}=\textstyle\sum_j\nu_j\mathrm{BW}_j,\ \ u=\sum_j\nu_j u_j,\ \ \sum_j\nu_j=1\ \ [\text{bandwidth menu},\ u_j=1/\mathrm{BW}_j]\\[-1pt]
&\textstyle\sum_a\alpha_a=1\ \text{(array aspect)},\quad \sum_f\phi_{\mathrm{ph},f}=1\ \text{(DVFS)},\quad \psi_{a,f}\ \text{joint one-hot of }(\alpha_a,\phi_{\mathrm{ph},f})\\[-1pt]
&\mathrm{BW_{eff}}\le\mathrm{BW},\quad \mathrm{BW_{eff}}\le B_{\mathrm{LD}}\ \text{(load engines) \citep{spector2024thunderkittens}}\\[-1pt]
&\kappa_{\mathrm{MAC}}\mathrm{MACs}+\kappa_{\mathrm{BW}}\mathrm{BW}+\kappa_{\mathrm{LD}}B_{\mathrm{LD}}+\kappa_{\mathrm{SRAM}}C_{\mathrm{GM}}\le A_{\mathrm{cap}}\\[-1pt]
&\pi_{\mathrm{MAC}}\mathrm{MACs}+\pi_{\mathrm{bit}}\,\varrho_{\mathrm{DRAM}}\le P_{\mathrm{cap}};\quad \text{6 manufacturability gates}
\end{align*}
\end{minipage}%
\hfill
\vrule
\hfill
\begin{minipage}[t]{0.485\linewidth}
\underline{\textsc{Capacity, residency, rematerialization} \citep{dao2022flashattention}}
\vspace{-2pt}
\begin{align*}
&\textstyle\sum_{e\in\mathcal E_{\mathrm{live}}(t)}\!\sum_c s^{\mathrm{KV}}d^{\mathrm{act}}_{e,c}z^{\mathrm{act}}_{e,c,t}
 +\sum_{i}\sum_c s^{W}d^{W}_{i,c}z^{W}_{i,c,t}\le\textstyle\sum_k 2^k h_{\mathrm{GM},k}\\[-1pt]
&p_{e,t}\le p_{e,t-1}+r_{\mathrm{src}(e),t},\qquad r_{i,t}\le\min(p_{e,t},p^W_{i,t})
\end{align*}
\vspace{-8pt}

\underline{\textsc{Precision} (per-phase, per-expert) \citep{lin2024awq,frantar2023gptq,hooper2024kvquant}}
\vspace{-2pt}
\begin{align*}
&\textstyle\sum_\rho q_{\phi,\rho}=1,\ \ s^{W}_\phi=\sum_\rho b^W_\rho q_{\phi,\rho},\ \ s^{\mathrm{KV}}_\phi=\sum_\rho b^{\mathrm{KV}}_\rho q_{\phi,\rho}
\end{align*}
\vspace{-8pt}

\underline{\textsc{KV budget / eviction $\to$ fit in CGM} \citep{xiao2024streamingllm,zhang2023h2o}}
\vspace{-2pt}
\begin{align*}
&B^{\mathrm{KV}}=\textstyle\sum_{\rho,s}B_{\rho,s},\ \ B_{\min}\pi_{\rho,s}\le B_{\rho,s}\le L_{\mathrm{ctx}}\pi_{\rho,s}\ \ [s\in\{\mathrm{on},\mathrm{off}\}]\\[-1pt]
&d^{\mathrm{KV}}\textstyle\sum_\rho b^{\mathrm{KV}}_\rho B_{\rho,\mathrm{on}}\le\sum_k 2^k h_{\mathrm{GM},k}\quad[\text{on-chip branch; hull, no big-}M]
\end{align*}
\vspace{-8pt}

\underline{\textsc{MoE expert residency} (cache / offload)}
\vspace{-2pt}
\begin{align*}
&\textstyle\sum_e g_e\,s^{W}_e W_e\le C_{\mathrm{HBM}},\qquad
 \Delta^{\mathrm{moe}}=\sum_e f_e(1-g_e)\,s^{W}_e W_e
\end{align*}
\vspace{-8pt}

\underline{\textsc{Prefix / KV cache reuse (RadixAttention)} \citep{zheng2024sglang}}
\vspace{-2pt}
\begin{align*}
&\widehat C^{\mathrm{pre}}=(1-\eta_{\mathrm{hit}}(C_{\mathrm{cache}}))\,C^{\mathrm{pre}},\quad
 \widehat M^{\mathrm{pre}}=(1-\eta_{\mathrm{hit}})\,M^{\mathrm{pre}}\quad[\text{prefill only}]
\end{align*}
\vspace{-8pt}

\underline{\textsc{Diffusion cross-step reuse (DeepCache)} \citep{ma2024deepcache}}
\vspace{-2pt}
\begin{align*}
&\widehat{\mathrm{cost}}_b=(1-\chi_b(1-\tfrac1W))\,\mathrm{cost}_b,\quad \textstyle\sum_b\chi_b F_b\le\sum_k 2^k h_{\mathrm{GM},k}
\end{align*}
\vspace{-8pt}

\underline{\textsc{Domains}}
\vspace{-2pt}
\begin{align*}
&y,p,p^W,r,w,z,h,q,\alpha,\phi,\psi,\nu,\pi,g,\chi\in[0,1];\ \ v,u,B_{\mathrm{LD}},B^{\mathrm{KV}},C_{\mathrm{cache}},C_{\mathrm{HBM}}\ge 0
\end{align*}
\end{minipage}

\vspace{2pt}
\end{minipage}}
\end{center}

\clearpage
\begin{center}
\footnotesize
\setlength{\arraycolsep}{1.5pt}
\fbox{%
\begin{minipage}{0.965\linewidth}
\centering
\vspace{2pt}
\textbf{\normalsize The Comprehensive CHEETAH-V1 Program: Extended Families}\\[2pt]
\emph{\scriptsize part of the program above; each family is written out, and each carries its validation status}\\[2pt]
\hrule
\vspace{4pt}
\scriptsize
\begin{minipage}[t]{0.485\linewidth}
\underline{\textsc{Structured sparsity} $2{:}4$} \hfill\emph{[measured, \cref{app:lp_provenance}]}
\vspace{-4pt}
\begin{align*}
&\textstyle\sum_\ell\sigma_\ell=1,\quad s^{\mathrm{spar}}=\sum_\ell(1-z_\ell)\,\sigma_\ell\ \ \text{scales }d^W\text{ and MACs}\\[-2pt]
&\kappa_{\mathrm{spar}}\textstyle\sum_{\ell>0}\sigma_\ell\le A_{\mathrm{cap}}\ \ [\text{sparse-MAC support costs area}]
\end{align*}
\vspace{-9pt}

\underline{\textsc{Memory-technology selector}} \hfill\emph{[measured, \cref{app:cvx_mapping}]}
\vspace{-4pt}
\begin{align*}
&\textstyle\sum_t m_t=1,\ \ (\kappa_{\mathrm{BW}},\pi_{\mathrm{bit}},\overline{\mathrm{BW}})=\sum_t m_t\,(\kappa^t_{\mathrm{BW}},\pi^t_{\mathrm{bit}},\overline{\mathrm{BW}}^t)\\[-2pt]
&\mathrm{BW}\le\textstyle\sum_t \overline{\mathrm{BW}}^t m_t\ \ [\text{hull of }m_t\mathrm{BW};\ \text{exact at integral }m]
\end{align*}
\vspace{-9pt}

\underline{\textsc{Compute--memory overlap} $\kappa$ (async copy / TMA)} \hfill\emph{[in the program]}
\vspace{-4pt}
\begin{align*}
&\tau_{i,c}\ge(1-\kappa)\,\mathrm{cmp}_{i,c}+\mathrm{mem}_{i,c},\qquad
 \tau_{i,c}\ge\mathrm{cmp}_{i,c}+(1-\kappa)\,\mathrm{mem}_{i,c}\\[-2pt]
&[\text{exactly }\tau\ge\mathrm{cmp}+\mathrm{mem}-\kappa\min(\cdot,\cdot)\text{ for a menu constant }0\le\kappa\le\kappa_{\max};\\[-2pt]
&\ \text{no min column, no McCormick credit; gated by an async-DMA area term}]
\end{align*}
\vspace{-9pt}

\underline{\textsc{Register file / SMEM} $C_{\mathrm{RF}}$ \textsc{and occupancy}} \hfill\emph{[in the program]}
\vspace{-4pt}
\begin{align*}
&\textstyle\sum_c \mathrm{tile}_{i,c}\,y_{i,c}\le C_{\mathrm{RF}},\qquad
 O_i=\textstyle\sum_c o_{i,c}\,y_{i,c},\qquad O_i\ge O_{\min}-\Lambda(1-\beta_i)\\[-2pt]
&\tau_{i,c}\ \ge\ \tau^{\mathrm{lat}}_{i,c}\,(1-\beta_i)\ \ [\text{DRAM latency hidden only at occupancy }O_{\min};\\[-2pt]
&\ \text{a tile}\leftrightarrow\text{latency-hiding trade-off distinct from }C_{\mathrm{GM}}]
\end{align*}
\end{minipage}%
\hfill
\vrule
\hfill
\begin{minipage}[t]{0.485\linewidth}
\underline{\textsc{Tiered KV residency} (hot on-chip / cold streamed)} \hfill\emph{[in the program]}
\vspace{-4pt}
\begin{align*}
&B_{\mathrm{hot}}\,s^{\mathrm{KV}}d^{\mathrm{KV}}\le\textstyle\sum_k 2^k h_{\mathrm{GM},k},\qquad 0\le B_{\mathrm{hot}}\le B^{\mathrm{KV}}\\[-2pt]
&\Delta^{\mathrm{KV}}=d^{\mathrm{KV}}\textstyle\sum_\rho b^{\mathrm{KV}}_\rho\big(B^{\mathrm{KV}}_\rho-B_{\mathrm{hot},\rho}\big)\ \ [\text{only the cold tail is streamed}]
\end{align*}
\vspace{-9pt}

\underline{\textsc{Winograd / transform support}} \hfill\emph{[in the program]}
\vspace{-4pt}
\begin{align*}
&\omega_i\in[0,1]\ \text{per }3{\times}3\ \text{conv},\qquad
 \mathrm{MACs}_i\times\big(1-\omega_i(1-r_{\mathrm{Wino}})\big)\\[-2pt]
&\kappa_{\mathrm{tr}}\,\max_i\omega_i\le A_{\mathrm{cap}}\ \ [\text{transform unit costs area; written as }\kappa_{\mathrm{tr}}\omega_i\le A_{\mathrm{cap}}\ \forall i]
\end{align*}
\vspace{-9pt}

\underline{\textsc{Explicit U-shape skip residency}} \hfill\emph{[in the program]}
\vspace{-4pt}
\begin{align*}
&\mathcal E_{\mathrm{live}}(t)\ \cup\!=\ \mathcal E_{\mathrm{skip}},\qquad
 p_{e,t}\le p_{e,t-1}+r_{\mathrm{src}(e),t}\ \ \forall e\in\mathcal E_{\mathrm{skip}}\\[-2pt]
&[\text{down-path features persist for their up-path consumers, so a long-range}\\[-2pt]
&\ \text{skip competes for }C_{\mathrm{GM}}\text{ against the sequential chain}]
\end{align*}
\vspace{-9pt}

\underline{\textsc{Scalability: shape aggregation}} \hfill\emph{[measured, \cref{app:shape_agg}]}
\vspace{-4pt}
\begin{align*}
&\text{build over }S\text{ unique op-shapes with multiplicities }n_s,\ \text{not }N\text{ operations}\\[-2pt]
&[{\sim}100\times\text{ size reduction; exact whenever identical shapes share a tile config}]
\end{align*}
\end{minipage}

\vspace{3pt}
\noindent\scriptsize\emph{Status key.} \emph{[measured]} marks a family solved and
reported in this paper. \emph{[in the program]} marks a family that is written
out above and carried by the formulation, and whose effect has not yet been
measured on the suite; it is inert when its selector is pinned off, so the
reported results are unchanged by its presence.
\footnotesize
\vspace{2pt}
\end{minipage}}
\end{center}

\vspace{4pt}
\noindent\small
Every constraint family reduces to a change in bytes moved, FLOPs, on-chip
residency, or a hardware parameter, hence a linear or convex-hull (disaggregated
McCormick) representable addition rather than an outer-loop guess. Fusion and on-chip tiling
realize FlashAttention-style IO-awareness \citep{dao2022flashattention}; the
interleaved objective and RadixAttention/paged prefix reuse come from vLLM and
SGLang \citep{kwon2023vllm,zheng2024sglang}; the load-engine vs.\ compute-worker
datapath split and native-MMA-tile utilization come from tile kernels
\citep{spector2024thunderkittens}; precision is bounded by an accuracy-vetted menu
\citep{lin2024awq,frantar2023gptq,hooper2024kvquant}; KV eviction follows
\citep{xiao2024streamingllm,zhang2023h2o}; and diffusion cross-step reuse follows
DeepCache \citep{ma2024deepcache}. Accuracy is never linearized; it is bounded by
the cited menu. Quantities that only rescale coefficients (the decode batch $N$,
the speculative-decode width $K$) enter as workload parameters, and genuinely
discrete or non-convex axes (interconnect radix, HBM-stack count, array aspect,
low-bit MAC-unit support, draft model, denoising-step schedule) remain columns the
LLM-Architect outer loop proposes and the LP scores by reduced cost against its
converged duals $\lambda_{\mathrm{BW}},\lambda_{\mathrm{MAC}},\lambda_{\mathrm{HBM}},
\lambda_{\mathrm{CGM}},\lambda_{\mathrm{LD}},\lambda_{\mathrm{cache}}$. Every added
block is inert when pinned, so the program subsumes the entire V0 and V1 hierarchy.

\end{landscape}
